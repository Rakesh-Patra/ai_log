# Secrets — never commit real credentials
.env
.env.*
!.env.example

# Kubernetes secrets (real values) — only templates belong in git
k8s/db-secret.yaml
k8s/redis-secret.yaml
!k8s/*-secret.example.yaml

*.pyc
project.lock.json
bin/
obj/
.vs/
node_modules/

# Local-only logs (do not commit)
agent_temp_logs.txt
