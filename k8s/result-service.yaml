apiVersion: v1
kind: Service
metadata:
  labels:
    app: result
  name: result
  annotations:
    traefik.ingress.kubernetes.io/service.sticky.cookie: "true"
spec:
  type: ClusterIP
  ports:
  - name: "result-service"
    port: 5001
    targetPort: 8080
  selector:
    app: result