# Docker Compose reads Postgres and related settings from agent/.env only.
# Copy the full template and fill in secrets locally:
#
#   cp agent/.env.example agent/.env
#
# Never commit agent/.env. See README.md → "Security before you push to GitHub".
